# 🌅 晨間報告 — 2026/07/09（過夜無人值守）

> 遵守過夜守則：只改 repo 程式與測試、全部 commit 到 `develop`、未合併 main、**未做任何實際部署**、未動 Google Sheets 真實資料。需人決策者一律未做、列於下方。

---

## 一、完成項目（依序，皆已 commit＋push 到 develop）

| # | 項目 | commit | 測試 |
|---|------|--------|------|
| 1 | **v3.2 主任務**：計價單日期戳（分群）＋代號前綴收回＋名稱式收回擋代號 | `09b150a`(版本)→`528c323`(回填) | run_v32 23 案 |
| 2 | **過夜A**：全路由回歸網 run_routes.js（11 路由一正一反＋6 查詢類） | `9cd3a98` | +45 案 |
| 3 | **過夜B**：路由衝突掃描（列清單，未改碼） | `fca5abd` | — |
| 4 | **過夜C**：晨間報告＋封版＋桌面 rc3.gs | 本次 | — |

### v3.2 三項修正內容
1. **計價單日期戳**：老闆 `#開啟日期戳`／`#關閉日期戳`／`#日期戳狀態`（分群，仿 #安靜）。開啟的群傳「數量*單價=金額」計價單（如 `119台*700=83,300`）→ 回當日日期「📅 2026/7/9」。**含 =金額 計價單一律不進寄運/台子**，修掉「119台」被判成台子誤出庫 119 的 bug。訊息照記群組訊息、統整金額掃 =金額 不漏（已測）。
2. **代號收回客戶名前綴**：`彰化芬園B*1收回` 可用；客戶與代號不符會擋（`陳記B*1收回`→警示不收）；`b*1收回` 照常。
3. **名稱式收回擋代號**：貼查詢結果加「收」時，若該品項已有代號紀錄 → 擋下請改用代號，**不再把 [A] 的貨一起誤收**（修實際發生過的整批誤收）。

## 二、測試結果
- **`node tests/golden/run_all.js` → 363/363 全綠**（14 套件；本輪新增 run_v32 23、run_routes 45）。
- 既有 `run.js` GREEN 3 / RED 2 為既有待決策 D4/D6，非回歸。

## 三、台子可疑誤記清單
- 測試環境無正式資料，**無法產出真實清單**。已提供唯讀診斷指令 **`#台子異常掃描`**（限老闆，只列不改）。
- 👉 **請老闆部署後在群組打一次 `#台子異常掃描`**：會列出台子庫存中「出庫 ≥30」的可疑列（疑似計價單被誤判台子出庫）。系統不會自動更改，請人工核對後決定是否手動修正試算表。

## 四、待你決策事項（過夜守則：未自行修改）
詳見 `docs/路由衝突掃描_v3.2.md`。摘要：
- **C1（影響較大）**：`客戶 台子*N`（如 `陳記 台子*3`）目前被記成**鐵架**不是台子（既有行為，非本次造成）。正確台子出庫請用 `客戶⏎品名 特 N件（台子）`。要不要改路由？**有回歸風險，待你決定**。
- **C2**：無乘號的 `10箱=5000` 不會日期戳（樣式要求要有 `*`）。要不要放寬樣式？
- **C3**：含「收款」二字的疑問句（`客戶收款人是誰`）會跳收款提示且不受安靜管制（v3.1 調查已列）。要不要讓該提示也尊重安靜？
- 另：安靜模式警示點清單見 `docs/安靜模式警示點調查_v3.1.md`（A 類危險確認建議保留、B 類防呆可討論）。

## 五、部署步驟（你來做，我不自動部署）
1. 貼桌面 `卡比集機器人-rc3.gs` 全文 → GAS → 管理部署 → 編輯 → 版本選「**新版本**」→ 部署（**勿按新增部署**，網址會變）。
2. 群組打 `#版本` 確認顯示 **v3.2 (09b150a)**。
3. 抽測：
   - `#開啟日期戳` → 傳 `台北素茵⏎高山初秋 特(修清) 10K 119台*700=83,300` → 應回 📅 日期、`查台子` 不增加。
   - `#台子異常掃描` → 看有沒有歷史誤記。
   - 出鐵架 `陳記 勝山鐵架*2` → 看 `[A]`；`彰化芬園B*1收回`（客戶名前綴）。
   - 回歸抽測：收款人指定、`#已收R0001`、寄運單、打卡。
4.（若尚未啟用每日備份）GAS 執行 `setupBackupTrigger` 一次。

## 六、一致性
- 桌面 `卡比集機器人-rc3.gs` 的 sha256 = git HEAD 的 `.gs`（`8da2542…`），已對齊。
- `develop` 與 `origin/develop` 同步；無未 commit 變更。
