# Bug1（1828 誤判寄運）LINE 實測腳本 — v2.58

> 部署後在 **管理群組** 實測。feature 分支 `feature/bug1-1828-misclassify`，驗收通過才 merge。

## A. 確認新版已生效
1. 打 `#版本` → 應回含 **「✅ 卡比集機器人 v2.58 …【Bug1寄運誤判】1828／首行純數字不再被誤記為寄運」**。
   （若還是 v2.57 = 沒重新部署，回 GAS「管理部署作業→新版本→部署」）

## B. 主測（Bug1 必須修好）
2. 送兩行訊息：
   ```
   1828
   翠峰文科高山228 中15件
   ```
   → **不應**回「✅ 已記錄寄運資料」；查旭陽寄運/「翠峰…寄運資料」**不應**出現這筆。（修好前會誤記）

## C. 回歸（絕對不得退步）
3. 正常寄運（換行格式）仍正常：
   ```
   玉美加工廠
   毛路87台
   寄旭陽
   ```
   → 應回「✅ 已記錄寄運資料」，玉美加工廠底下有 毛路87台、物流＝旭陽。
4. 平常慣用的寄運/出貨單再測 2–3 筆真實例子 → 都要正常記錄、金額品項無誤。
5. 市場群組送寄運 → 仍不得寫入（唯讀）。

## D. 判定
- B 過 + C 全部不變 → **驗收通過** → 通知 CTO：merge develop→main、打 tag `v2.58-stable`。
- 任一異常 → 截圖回報。正式版不受影響（fix 在 feature 分支未 merge）；必要時 `scripts\rollback.ps1 -Tag v2.57-stable`。

## 附：自動護欄結果（供參考）
`node tests/golden/run.js`：G001（1828）**GREEN**、G004n（換行玉美）GREEN、G007（市場群組）GREEN。
（G004 單行格式、G005 打卡仍 RED＝屬另案，非 Bug1。）
