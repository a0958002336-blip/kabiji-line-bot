# Golden Regression Test — 測試清單（MANIFEST）

> 每次修改 / 合併 / 部署前**必跑**。任一項失敗 → **禁止 merge**。
> Owner：B2（QA）。GAS 側以 node mock 執行。
> 狀態：`spec`=規格已定待實作；`impl`=已實作可跑。

| # | 測試 | Given → When → Then | 對應規則 | 可否 node 單測 | 狀態 |
|---|---|---|---|---|---|
| 001 | 一般交易不建寄運 | `1828`⏎`翠峰文科高山228 中15件` → classify → **非寄運**，`寄運資料`不新增 | R1.1 | ✅ 純分類/parser | spec |
| 002 | 收錢後未收款消失 | 客戶`6986`未收→`6986 收錢`→`#未收款` → 該客戶不再出現 | R2.1 | ⚠️ 需對帳模型(新建) | spec(blocked) |
| 003 | 回覆取消成功 | 回覆某寄運訊息輸入`取消` → 該筆刪除/標記取消 | R3.1 | ⚠️ 需 quote 上下文模擬 | spec |
| 004 | 玉美加工廠寄旭陽 | `玉美加工廠 毛路87台 寄旭陽` → `寄運資料`新增(客戶=玉美加工廠,品項=毛路,數量=87,包裝=台子,物流=旭陽) | R1.3 | ✅ 純 parser | spec |
| 005 | 錯誤打卡不入正式 | 亂碼`XXX 下班`(非員工) → 不寫`attendance_logs`，導入`abnormal_logs` | R4.1 | ✅ 純驗證函式 | spec |
| 006 | 冰庫出貨扣庫存 | `【客戶】品名：N 出M` → `冰庫寄存`餘額 -M | R6.1 | ✅ 邏輯可測 | spec |
| 007 | 市場群組不得改寄運 | 市場群組送寄運寫入 → 拒絕、不寫 | R5.1 | ✅ getPerm 可測 | spec |
| 008 | APP 登入正常 | APP 帶正確憑證 → 登入成功 | ERP | ❌ ERP repo | spec(ERP) |
| 009 | APP UI 改動不影響 LINE | 改 mobile/web → 跑 LINE 全指令回歸 → 全綠 | 隔離 | 部分 | spec |
| 010 | LINE 原有指令不失效 | 全指令回歸（寄運/冰庫/台子/鐵架/借支/打卡/查詢…） | 回歸 | ✅ | spec |

## 執行方式（草案）
```
node tests/golden/run.js          # 跑全部，任一紅 exit 1（供 pre-push hook 擋 merge）
```

## 實作優先序（B2）
1. 先做純函式可測的：001, 004, 005, 007（不需模擬 GAS I/O，最快建立護欄）。
2. 006（庫存邏輯）、010（回歸）。
3. 003（需 quote 模擬）、002（等 R2 對帳模型定案）、008/009（ERP/APP 側）。

> 註：001/004/005/007 是**把現有正確行為與待修目標行為都寫成斷言**——修 bug 前先讓「錯誤現況」的測試轉紅，修完轉綠，即為驗收證據。
