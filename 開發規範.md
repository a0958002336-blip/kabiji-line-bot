# 卡比集機器人 — 開發規範（正式版／開發版分流制度）

> 建立日期：2026-06-29　維護：CTO（Release Manager）

## 一、版本分流

| 版本線 | 檔案 | 用途 | 規則 |
|--------|------|------|------|
| **正式營運版 v2.5-stable** | `卡比集機器人_核心完整版.gs`（部署檔 `Desktop\卡比集機器人_v2.55.gs`）；凍結備份 `GAS_v2.5_stable.gs`、`GAS_v2.55_stable.gs`；git tag `v2.5-stable`/`v2.55-stable` | 公司每日正式使用 | **只准修 Bug**；不准新增功能；不准大改 Router/Parser/doPost/LockService；改前必備份、改後跑 Regression Gate、LINE 實測 PASS 才部署 |
| **開發版 v2.6-dev** | `卡比集機器人_v2.6-dev.gs`；git 分支 `v2.6-dev` | ERP/AI/圖片辨識/新指令/新模組/架構重構/新流程測試 | **不得直接影響正式 LINE 機器人**；所有新功能在此驗證 |

> 🔴 **正式版不可直接新增功能。** 任何新功能一律進 v2.6-dev，穩定後由 CTO 評估才合併到下一個 stable。

## 二、角色

- **CTO**：版本管理、Merge、部署、回滾（唯一可 Merge / 可建立 GAS 新版本者）。
- **A 工程師**：只修 鐵架 / 冰庫 / 借支。
- **B 工程師**：只修 寄運 / 修清 / 1052 / 幸福舊廠。
- **QA**：只做測試，不改程式。
- 禁止跨模組修改。

## 三、核心模組保護
Router / doPost / handleEvent / Parser / Dispatcher / LockService —— 任何修改須先向 CTO 報變更原因，核准後才動。

## 四、單次部署範圍
單次修改若影響 **> 3 個模組**，CTO 必須拆成多個版本分批部署；每次部署範圍盡量單純，便於定位與回滾。

## 五、Merge 前檢查（缺一不可）
☐ node --check PASS　☐ Regression Gate PASS　☐ 無 conflict　☐ CHANGELOG 完整　☐ Backup 完成

## 六、Regression Gate 固定項目
查冰庫 / 查鐵架 / 借鐵架 / 收鐵架 / 查全部鐵架 / 查XX鐵架 / 寄運寫入 / 寄運取消 / 查寄運 / 查詢速度 / 管理群組可寫 / 市場群組唯讀。
（修清/幸福舊廠/1052 屬資料與備註顯示，非程式功能，不列為功能測項。）

## 七、Bug 治理
每修一個 Bug：記 Root Cause → 加 Regression Test → 更新 CHANGELOG。

## 八、Release 流程
工程師完成（工作版）→ CTO Review → Merge → Regression Gate → 建新版本 → GAS 部署（使用者手動）→ 使用者 LINE 實測 → 結案。

## 九、實測失敗處理
正式版 LINE 實測若有問題：**不直接改正式版**，先建立 Issue（記 Root Cause），由 CTO 評估 Hotfix 或納入 v2.6。

## 十、部署限制（現實）
CTO（本機助理）**無法代按 GAS 部署**（無 clasp/GAS API 連線，且不碰 Token/Webhook）。CTO 完成 Merge/Gate/版本/備份/部署檔/CHANGELOG，實際 GAS「新版本→部署」由使用者手動執行。
