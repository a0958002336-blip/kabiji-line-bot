# TODO — 卡比集機器人 + APP（更新：2026-08-09）

## 🆕 v3.5 規格討論中（2026/08/08 老闆提出，尚未動工）

### A. 叫貨分店標記
台中店／仁德店混在同一叫貨群，靠「誰發話」分店；訊息極簡（例「高麗3」，無貨主/單位/客戶）。
需新分頁對照 LINE userId → 所屬店，回覆標「台中店下單」。
**設計要點（已與老闆討論）**：極簡輸入必須三層 fail-closed（登記過的叫貨群 ＋ 嚴格
`^品名+數字$` ＋ **品名白名單**）才能與閒聊區分，只靠群組來源不夠；對照表綁 **userId 不綁
displayName**（現有 EMP_BINDINGS 綁 displayName 是弱點，改暱稱就斷）；未登記者 fail-closed
不寫入、自動登錄待補、回覆＋通知老闆。
**待老闆決定**：叫貨單位為何（各品項是否不同）／品項數量與變動頻率／資料寫哪張表／
是否多行多品項／同一人是否可能跨店／叫貨群是否設為管理群組（不設會被唯讀閘門擋掉）。

### B. 寄運跨群推播
送貨群寫入寄運成功後，自動 push 一則彙整到「寄運群」（一單一則，不洗版）。
**設計要點**：`#群組ID` 已現成、`pushToAdminGroups()` 可當範本；推播第一行放純日期，
可讓內容被貼回時被 `isShippingPullOutput()` 認出而不重複記錄；push 失敗絕不可影響試算表寫入
（先寫後推、包 try/catch、檢查 response code —— 現有 `pushToAdminGroups` **沒檢查回應碼**是既有缺口）。
**阻塞項**：`reply` 免費但 `push` 計費，須先用 `GET /v2/bot/message/quota` 與
`/quota/consumption` 實測配額、並確認「推一則到群」是算 1 則還是按人頭計。
**若按人頭計 → 架構要改成定時彙整而非即時推**，所以配額未確認前 B 不定案。

### C. `#備份狀態` 指令（小項，可自主推進）
每日備份目前**沒有任何 LINE 指令可查狀態**（電錶有 `#電錶提醒`，備份沒有），
導致「以為有在跑其實沒跑」只能靠人工去 Drive 看。
建議比照 `meterReminderStatus()` 做一支：查 `dailyBackup` 觸發器是否存在 ＋
最近一次備份日期 ＋ 清理提醒上次發送時間。唯讀、零風險、成本小。
（詳見 `docs/備份與還原手冊.md` 5-5）


## 🔴 等董事長回家一次決策（見 docs/DECISIONS_PENDING.md）
- D1 收款/未收款資料模型（Bug2）
- D2 取消流水號 schema（Bug3）
- D3 冰庫實際問題描述（你說「部署後沒變化」，但診斷顯示冰庫扣庫存正常）
- D4 打卡 abnormal_logs schema + 修正打卡語義（Bug4）
- D5 效能：出貨迴圈冰庫 map-hoist（較侵入，需核可）
- D6 寄運輸入格式：單行 vs 多行（Golden G004）

## 🟢 可自主推進（不需決策）
1. **Bug1 驗收閉環**：等你部署 v2.58 + LINE 實測（腳本已備）。
2. **Golden 補齊**：新增 出貨/冰庫/台子/鐵架/查詢 回歸案例（B2，需 seeded mock data）。
3. **APP Phase 1**：C 正式開發中（登入/首頁/查詢/庫存/冰庫/未收款/寄運，接真實 API，feature 分支）。
4. **UI Prototype 完善**：依你回來的 6 個設計選擇調整。
5. **Bug5 權限缺口**：低風險，可先修（feature 分支 + Golden）。
6. **文件/CHANGELOG/Regression** 持續維護。

## 🟡 未來排程（董事長已指示：排未來、現階段先不做）
- **自由交易解析（員工多行交易記錄）**：員工現用自創格式回報一整筆買賣＋收款，例：
  ```
  2859/黑網
  進口山東 *5
  450/2250元
  已收2250元
  ```
  目前機器人**無此解析功能**（寄運需「件＋寄物流」、收款需 #、鐵架需「鐵架」；此格式三者皆不符），v3.4.2 起一律**裝死無視**。
  待評估：是否新增「客戶／品名＋名稱*數量＋單價/總價＋已收X元」的交易入帳解析，或維持無視由員工改用正式指令。
  決策未定前：**先教育員工用正式格式**（董事長 2026-07-15 指示）。哲學：明確關鍵字才動作，格式相似不足以觸發。

## ⛔ 明令不做（等你決策）
- 不改 Business Rules、不改 Schema、不改 Message Router 大結構、不動正式資料。
